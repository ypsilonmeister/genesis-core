pub enum Expr {
    Literal(i32),
}

pub enum BinOp {
    Add,
}

pub enum UnaryOp {
    Neg,
}

enum Task<'a> {
    Eval(&'a Expr),
    ApplyBinOp(BinOp),
    ApplyUnaryOp(UnaryOp),
    ApplyFunc(String, usize), // name, arg_count
}

fn main() {}